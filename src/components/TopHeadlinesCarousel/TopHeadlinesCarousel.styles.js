import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  root: {
    alignItems: 'center',
    flex: 0,
  },
  root__animatedItem: {
    marginHorizontal: 15,
    paddingHorizontal: 8,
    alignItems: 'center',
  },
  root__itemImage: {
    width: '100%',
    resizeMode: 'cover',
    borderRadius: 24,
    margin: 0,
  },
});
